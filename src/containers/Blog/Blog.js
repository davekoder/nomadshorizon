import React from 'react'
import classes from './blog.module.scss'

const Blog = () => {
  return (
    <div
      className={`${classes.blog} flex flex-col w-screen h-screen overflow-x-hidden`}></div>
  )
}

export default Blog
